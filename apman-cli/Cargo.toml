[package]
name = "apman-cli"
version.workspace = true
edition = "2021"

[[bin]]
name = "apman"
path = "src/main.rs"

[dependencies]
apman-core = { path = "../crates/apman-core" }
apman-device = { path = "../crates/apman-device" }
clap = { version = "4", features = ["derive"] }
